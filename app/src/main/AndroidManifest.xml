<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 网络权限 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- 位置权限 -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- 摄像头权限 -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- 存储权限 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- 拨打电话权限 -->
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.CALL_PRIVILEGED" />

    <!-- 发送短信权限 -->
    <uses-permission android:name="android.permission.SEND_SMS" />

    <!-- 读取短信权限 -->
    <uses-permission android:name="android.permission.READ_SMS" />

    <!-- 录音权限 -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- 震动权限 -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- WiFi权限 -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- 唤醒锁 -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- 前台服务权限 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <!-- 硬件特性 -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.location.gps"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.microphone"
        android:required="false" />

    <application
        android:name=".SafetyAssistantApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SafetyAssistant"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <meta-data
            android:name="com.amap.api.v2.apikey"
            android:value="68292ff70db1d52e4eff7f4f7846c478" />

        <!-- 主界面 -->
        <activity
            android:name=".activities.MainActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 用户界面 -->
        <activity
            android:name=".activities.UserActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- 直播界面 -->
        <activity
            android:name=".activities.LiveActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- 文本检测界面 -->
        <activity
            android:name=".activities.TextCheckActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- 二维码扫描界面 -->
        <activity
            android:name=".activities.QRScanActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- 地图导航界面 -->
        <activity
            android:name=".activities.MapActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- 安全模式界面 -->
        <activity
            android:name=".activities.SafeModeActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- 语音助手界面 -->
        <activity
            android:name=".activities.VoiceActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- 历史记录界面 -->
        <activity
            android:name=".activities.HistoryActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- 反诈游戏界面 -->
        <activity
            android:name=".activities.GameActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- 课程播放界面 -->
        <activity
            android:name=".activities.LivePlayerActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

    </application>

</manifest>
